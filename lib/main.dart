import 'package:flutter/material.dart';
import 'app.dart';

/// Entry point of the application.
void main() {
  // Ensure Flutter binding is ready before running the app.
  WidgetsFlutterBinding.ensureInitialized();
  runApp(const App());
}
